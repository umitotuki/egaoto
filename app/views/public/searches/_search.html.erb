<% if user_signed_in? %>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({"User" => "user", "Post" => "post"}) %>
    <%= f.select :range, options_for_select({"部分一致" => "partial"}) %>
    <%= f.submit "検索" %>
  <% end %>
<% end %>