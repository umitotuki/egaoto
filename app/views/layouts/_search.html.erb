<% if user_signed_in? %>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :search %>
    <%= f.select :model, options_for_select({"ユーザー" => "user", "投 稿" => "post"}) %>
    <%= f.submit "検索" %>
  <% end %>
<% end %>